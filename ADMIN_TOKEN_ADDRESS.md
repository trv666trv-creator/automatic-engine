# 🔧 Как изменить адрес токена (для админа)

## 🌐 Глобальное сохранение адреса

Адрес токена хранится в файле `token-address.json` на GitHub. Все пользователи видят один и тот же адрес.

---

## 📝 Как изменить адрес:

### Способ 1: Через GitHub (рекомендуется)

1. Перейдите в репозиторий:
   https://github.com/trv666trv-creator/automatic-engine

2. Откройте файл `token-address.json`

3. Нажмите кнопку **✏️ Edit** (редактировать)

4. Измените адрес в поле `mintAddress`:
   ```json
   {
     "mintAddress": "ВАШ_НОВЫЙ_АДРЕС_ТОКЕНА"
   }
   ```

5. Внизу страницы:
   - Commit message: `Update token address`
   - Нажмите **"Commit changes"**

6. ✅ Готово! Адрес обновится для всех пользователей в течение 1-2 минут

---

### Способ 2: Через локальное редактирование

1. Откройте файл `token-address.json` в редакторе

2. Измените адрес:
   ```json
   {
     "mintAddress": "ВАШ_НОВЫЙ_АДРЕС_ТОКЕНА"
   }
   ```

3. Сохраните файл

4. Загрузите на GitHub:
   ```bash
   git add token-address.json
   git commit -m "Update token address"
   git push
   ```

---

## ⚡ Быстрая инструкция:

```
1. GitHub → automatic-engine → token-address.json
2. Edit → Изменить адрес → Commit
3. ГОТОВО! Все пользователи увидят новый адрес
```

---

## 🔄 Как это работает:

1. Сайт читает `token-address.json` с GitHub
2. Адрес из JSON показывается всем пользователям
3. При изменении файла адрес обновляется для всех

---

## 📋 Формат файла:

```json
{
  "mintAddress": "AGdGTQa8iRnSx4fQJehWo4Xwbh1bzTazs55R6Jwupump"
}
```

**Важно:** 
- Адрес должен быть в кавычках
- Без запятой в конце
- Строго формат JSON

---

**После первого запуска токена обновите адрес в `token-address.json`!** 🚀
